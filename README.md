# smart-braille-reader
A simple Python-based system that reads Braille from images and converts it into text. It detects Braille dots using image processing and groups them into characters. The result is displayed through an easy-to-use GUI.
